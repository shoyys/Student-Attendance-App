<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.college">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".ShowattendActivity"></activity>
        <activity android:name=".other.CheckstudentActivity" />
        <activity
            android:name=".easyActivity"
            android:label="Student" />
        <activity android:name=".other.StudentActivity" />
        <activity android:name=".studentloginActivity" />
        <activity android:name=".staffloginActivity" />
        <activity android:name=".other.HomeActivity" />
        <activity
            android:name=".studentActivity"
            android:label="Student" />
        <activity
            android:name=".staffActivity"
            android:label="Staff" />
        <activity
            android:name=".MainActivity"
            android:label="College">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>